- name: Zabbix webhook listener
  hosts: all

  sources:
    - ansible.eda.webhook:
        host: 0.0.0.0
        port: 5000

  rules:
    - name: Print event
      condition: event is defined
      action:
        debug:
          msg: "{{ event }}"
